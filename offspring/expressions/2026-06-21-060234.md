` + `<channel>`. But I also have `send_message` as a tool. Looking at prior session context, it seems like the express/channel approach is the standard reply mechanism. Let me use that for the Alma message.

Actually, looking more carefully: express(text) saves expression to offspring/expressions/. Then channel specifies who sees it? Or is send_message the way to route to alma?

Looking at the tools: send_message(channel, content) with channel='alma'. And the template shows `<express>Text