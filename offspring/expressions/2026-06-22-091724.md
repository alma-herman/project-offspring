` with `<channel>` for the response. Let me use channel 'alma' since this is a response to Alma's tick.

Actually wait — looking at the tool description: `send_message(channel, content)` sends messages to channels. But the format also has `<express>Text